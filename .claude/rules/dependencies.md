# Rule — dependencies and packages

> **Moved.** The body of this rule now lives in
> [`.github/instructions/dependencies.instructions.md`](../../.github/instructions/dependencies.instructions.md),
> so Claude Code and GitHub Copilot read the same prose instead of two copies that drift.
> Edit that file — this one is a redirect.

Central Package Management, justifying a new package, layer discipline for package references.

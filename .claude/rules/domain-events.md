# Rule — domain events and the outbox

> **Moved.** The body of this rule now lives in
> [`.github/instructions/domain-events.instructions.md`](../../.github/instructions/domain-events.instructions.md),
> so Claude Code and GitHub Copilot read the same prose instead of two copies that drift.
> Edit that file — this one is a redirect.

[DomainEventName] is an immutable wire contract; event payload shape, the outbox, at-least-once delivery and idempotency.

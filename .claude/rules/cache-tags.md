# Rule — cache tags are constants, never literals

> **Moved.** The body of this rule now lives in
> [`.github/instructions/cache-tags.instructions.md`](../../.github/instructions/cache-tags.instructions.md),
> so Claude Code and GitHub Copilot read the same prose instead of two copies that drift.
> Edit that file — this one is a redirect.

Cache tags are constants from Server.Shared/<Context>/CacheTags.cs, never string literals.

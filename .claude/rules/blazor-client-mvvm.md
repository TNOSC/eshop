# Rule — Blazor client: pages compose, components own a ViewModel + service

> **Moved.** The body of this rule now lives in
> [`.github/instructions/blazor-client-mvvm.instructions.md`](../../.github/instructions/blazor-client-mvvm.instructions.md),
> so Claude Code and GitHub Copilot read the same prose instead of two copies that drift.
> Edit that file — this one is a redirect.

Blazor pages compose only; components own a colocated ViewModel and service; the shared ClientValidation helper.

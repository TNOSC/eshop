# Rule — EF Core migrations

> **Moved.** The body of this rule now lives in
> [`.github/instructions/migrations.instructions.md`](../../.github/instructions/migrations.instructions.md),
> so Claude Code and GitHub Copilot read the same prose instead of two copies that drift.
> Edit that file — this one is a redirect.

Two-context dotnet ef mechanics, reviewing the generated file, destructive changes, never editing an applied migration.

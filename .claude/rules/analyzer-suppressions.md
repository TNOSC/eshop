# Rule — analyzer suppressions

> **Moved.** The body of this rule now lives in
> [`.github/instructions/analyzer-suppressions.instructions.md`](../../.github/instructions/analyzer-suppressions.instructions.md),
> so Claude Code and GitHub Copilot read the same prose instead of two copies that drift.
> Edit that file — this one is a redirect.

When a #pragma or .editorconfig entry is acceptable; which suppressions are settled; what may never be suppressed.

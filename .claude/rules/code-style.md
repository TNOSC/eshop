# Rule — code style

> **Moved.** The body of this rule now lives in
> [`.github/instructions/code-style.instructions.md`](../../.github/instructions/code-style.instructions.md),
> so Claude Code and GitHub Copilot read the same prose instead of two copies that drift.
> Edit that file — this one is a redirect.

File header and layout, primary constructors, named arguments, one parameter per line past two, Async naming, error-code and ErrorType conventions.

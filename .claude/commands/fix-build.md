---
description: Triage a failing build — group warnings-as-errors by rule, fix the cause rather than suppressing the symptom
argument-hint: "[optional: a specific rule ID or project to focus on, e.g. CA1852 or Server.Api]"
---

> **Body moved.** The steps for this command now live in
> [`.github/skills/fix-build/SKILL.md`](../../.github/skills/fix-build/SKILL.md), so Claude Code and
> GitHub Copilot run the same workflow instead of two copies that drift.

Read that file and follow it step by step. `$ARGUMENTS` is what the user typed after the command.

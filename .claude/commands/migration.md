---
description: Add an EF Core migration with the correct two-context flags, then review the generated file before it is trusted
argument-hint: <MigrationName, PascalCase_With_Underscores — e.g. Add_Reviews>
---

> **Body moved.** The steps for this command now live in
> [`.github/skills/migration/SKILL.md`](../../.github/skills/migration/SKILL.md), so Claude Code and
> GitHub Copilot run the same workflow instead of two copies that drift.

Read that file and follow it step by step. `$ARGUMENTS` is what the user typed after the command.

---
description: Run the full definition-of-done gate — build plus the architecture, unit and integration suites — and report honestly
argument-hint: "[optional: suite to limit to, e.g. \"architecture\" or \"unit\"]"
---

> **Body moved.** The steps for this command now live in
> [`.github/skills/verify/SKILL.md`](../../.github/skills/verify/SKILL.md), so Claude Code and
> GitHub Copilot run the same workflow instead of two copies that drift.

Read that file and follow it step by step. `$ARGUMENTS` is what the user typed after the command.
